Drop your Stalky character images here using these exact filenames.
The UI auto-detects what's present and falls back to a 👀 emoji for any that are missing.

REQUIRED (or the bot uses emoji):
  stalky.png            — default avatar (front-view, used everywhere by default)

OPTIONAL POSES (used contextually if present):
  stalky-thinking.png   — shown while the bot is typing
  stalky-excited.png    — shown for High Conviction analyses
  stalky-sad.png        — shown when an analysis fails
  stalky-wink.png       — shown in the welcome message
  stalky-full.png       — full-body, shown in the welcome hero card

NOTES:
- PNG with transparent background works best (no halo around the avatar circle).
- For round avatar usage the image is cropped focusing on the face — so a head-and-shoulders or full body composition both work.
- Dimensions: anything from 256x256 up to 1024x1024 is fine. Keep file size under ~500KB for fast loading.
- If you want the hoodie to say "stalky" instead of "stalk." — that change has to happen in the source image (re-render in your image tool of choice). I can't edit pixels for you.
